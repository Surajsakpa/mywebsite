Options -Indexes
Options +FollowSymLinks

# Block data folder from web access
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^data/ - [F,L]
</IfModule>

# Allow PHP to run
AddHandler application/x-httpd-php .php
